name: AboutMe

on: [push]

jobs:
  build4test:
    runs-on: ubuntu-latest

    steps:
    - name: apt install sphinx
      run : sudo apt install sphinx
    - name: cloning repository
      run : git clone https://github.com/Mitya178/Resume4Yadro.git && ls -la
    - name: make html
      run : ls -la && cd /Resume4Yadro && make html

 
